@model ApplicationCore.Models.Response.CastDetailsResponseModel


<h2>
    Cast Details
</h2>

<div class="row">
    <div class="col-md-4">


        <!-- Movie Card row -->
        <div>
            <img src="@Model.ProfilePath"
                 class="img-fluid rounded-circle"
                 alt="@Model.Name" />
        </div>

    </div>
    <div class="col-md-4">
        <div class="row mt-2">
            <div class="col-12">
                <h1>
                    @Model.Name
                </h1>
                <small class="text-muted"> @Model.Gender </small>
            </div>

        </div>

        <div class="row">
            <div class="col-4 text-secondary font-weight-bold mt-1">
                @Model.TmdbUrl
            </div>

        </div>

        <div class="container-fluid bg-light">
            <div class="row">
                @*@foreach (var movie in Model.Movies)
                                {
                    <div class="col-6 col-lg-3 col-sm-4 col-xl-2">
                        @await Html.PartialAsync("_MovieCard", movie)
                    </div>}*@
            </div>
        </div>
    </div>
</div>